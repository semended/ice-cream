# Codex Hooks

This directory is reserved for project-local Codex lifecycle hooks.

No hook script is active yet. Keep future hooks explicit, reviewable, and non-destructive. Do not add hooks that automatically commit, push, delete files, install dependencies, rotate secrets, or change the environment without confirmation.
